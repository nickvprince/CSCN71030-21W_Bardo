#include "shop.h"
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
bool Skilltree(entity*);
void printSkills(entity*);

void buySkill(entity*);
void resetSkill(entity*);
void resetAllSkills(entity*);
bool checkSkillsEmpty(entity*);