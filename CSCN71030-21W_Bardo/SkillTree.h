#include "Objects.h"
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
using namespace std;
bool Skilltree(user*);
void printSkills(user*);

void buySkill(user*);
void resetSkill(user*);
void resetAllSkills(user*);
bool checkSkillsEmpty(user*);